# GUI (flutter_orchestrator_gui.py)
customtkinter>=5.2.0

# Validação de pubspec.yaml nas CLIs (flutter_orchestrator.py).
# Opcional: sem ele, a validação de YAML fica limitada, mas o programa roda.
PyYAML>=6.0
